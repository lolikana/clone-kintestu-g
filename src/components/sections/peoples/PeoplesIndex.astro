---
import SectionLayout from '@layouts/SectionLayout.astro';
import { peoplesData } from '@libs/data';

import PeoplesCard from './PeoplesCard.astro';
---

<SectionLayout title="Peoples" subtitles={['働く社員の姿']}>
	<ul class="peoples__cards">
		{peoplesData.map(data => <PeoplesCard data={data} />)}
	</ul>
</SectionLayout>

<style lang="scss">
	@import '@styles/config';
	.peoples__cards {
		color: #000;
		display: flex;
		flex-direction: column;
		gap: 4rem;
	}

	@media screen and (min-width: 768px) {
		.peoples__cards {
			gap: 6rem;
		}
	}
</style>
